{
  "name": "aloe-ii-core",
  "version": "0.0.1",
  "author": "Aloe Labs, Inc.",
  "license": "GPL-3.0",
  "dependencies": {},
  "devDependencies": {
    "prettier": "^2.7.1",
    "prettier-plugin-solidity": "^1.0.0-dev.22"
  },
  "scripts": {
    "lint": "prettier --write **.sol",
    "test": "./forge.sh test -vv --no-match-contract Lens --gas-report --fork-url FOUNDRY_ETH_RPC_URL --fork-block-number 13806816",
    "test-events": "./forge.sh test -vv --match-contract Lens --fork-url FOUNDRY_ETH_RPC_URL --fork-block-number 14890000",
    "snapshot": "./forge.sh snapshot --no-match-contract Lens --fork-url FOUNDRY_ETH_RPC_URL --fork-block-number 13806816"
  }
}
